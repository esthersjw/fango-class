---
name: fango-class
description: 用于 ChatCut 中的中文口播教程、访谈和工具讲解视频。按“内容优先、包装后置”的顺序完成素材整理、编号逐字稿、内容审查、口癖与语义清理、专业知识疑点人工 Review、主视频剪辑、字幕重排，以及最后的外框、进度条和 Motion Graphics 对齐。适用于用户说剪口播、去水话、去口癖、审查逐字稿、按编号删句、做教程成片或整理可发布视频时。
---

# Fango Class

## 核心目标

把中文口播教程整理成适合社交媒体发布、且仍可在 ChatCut 中继续编辑的成片。

核心原则：

> 先确定“说什么”，再确定“怎么呈现”。

不要在主内容尚未锁定前添加依赖时间线的外框、进度条、章节卡片或 Motion Graphics。

## 硬性规则

- 先读取最新 ChatCut 项目、时间线、素材、字幕和已有视觉层；不依赖旧的 item id、旧时长或记忆中的位置。
- 用户已经手动调整过的画面、字幕字体、位置、外框、进度条和动画优先级最高，只做增量修改。
- 涉及“说了什么”的删除，使用 Script/逐字稿工作流；不要只删字幕，也不要用物理裁切替代语音脚本编辑。
- 口癖可以积极清理，但“然后 / 这个 / 就是 / 比如说”等必须结合语境，不做无脑全局替换。
- 专业知识疑点必须进入人工 Review：AI 只能指出问题、解释风险并给出方案，不能未经用户选择直接改写或删除。
- 内容审查和主视频剪辑完成后，才刷新字幕并添加外框、进度条和动画。
- 长时间 ChatCut 操作必须串行；超时后先确认后台状态，不并发重试写入。
- 默认交付可编辑 ChatCut 时间线；只有用户明确要求导出或下载时才导出视频。

## 工作流

### 1. 建立基线

读取并记录：

- 视频素材、顺序、时长、画面比例和播放速度
- 音频是否嵌入视频、是否已有独立音轨
- 当前字幕内容、样式、位置和分行规则
- 已有外框、进度条、开头动画、章节和注释层
- 用户已经手动改动的布局与视觉偏好

如项目已有视觉包装，不重置它。只在内容锁定后重新计算其时长和锚点。

### 2. 合并和统一基础参数

有多个视频时，按用户指定顺序合并；统一用户指定的播放速度，例如全部 1.1×；检查片段之间的黑屏、空档、重复片段、尾部残留和音画关系。

这一阶段只处理主视频和其音频，不添加新的包装层。

### 3. 生成编号版逐字稿

从当前字幕或已准备好的转录中生成 Markdown 逐字稿。每条必须包含：

```text
编号｜起始时间–结束时间｜原字幕
```

编号仅用于用户审查和后续指令；第一次输出时不擅自改写原文。保留专业词的原始识别结果，疑似错误另行标注。

### 4. 内容审查

逐条判断并归类为：

- **保留**：新知识、关键解释、具体演示、必要上下文、真实提问、困惑、情绪和理解过程。
- **删除**：没有新增信息的残句、重复观点、失败开头、无意义寒暄和不影响理解的无效回应。
- **缩短**：保留核心意思，只删掉前后的口癖、重复起句或无效铺垫。
- **专业知识疑点**：技术概念可能错误、过度简化、术语识别错误或画面与表述不一致。

输出审查表，至少包含：编号、时间、原话、判断、原因、影响和建议处理方式。用户明确说“删除第几条”时，可以把这些条目直接列入删除候选，但在正式剪辑前仍要确认其上下文不会被截断。

### 5. 口癖与语义清理

#### 可以积极删除

- “呃 / 额”以及无意义的“嗯 / 啊”
- 口吃、重复起句、失败后立即重说的前半句
- “我想想”“等一下”“刚才说到哪了”等无信息量停顿
- 没有作用的“对吧”“好不好”
- 连续重复的“就是 / 然后 / 这个 / 比如说”
- 不承担逻辑作用的“然后”“这个”“比如说”

#### 必须按语境判断

- “然后”如果表示顺序或推进，保留。
- “这个”如果指代具体项目、网页或文件，保留。
- “比如说”如果引出理解知识所必需的例子，保留；如果只是口头铺垫，可以删除。
- “就是”如果用于定义、强调或修正观点，保留；如果只是填充，可以删除。

删除后必须重新读一遍前后句，确认语法、逻辑和听感自然。优先删除连续完整的短语，不把不同时间的半句话强行拼接。

### 6. 专业知识人工 Review（强制门槛）

对每个疑点输出以下格式，并暂停等待用户判断：

```text
编号 / 时间：
原话：
疑点：
为什么可能有问题：
建议方案：
  A. 删除
  B. 保留原话
  C. 保留并加字幕注释
  D. 重新录音或补充旁白
等待决定：
```

不得因为“听起来不专业”就直接改写。用户选择方案后，才把决定转成 Script 删除、字幕修改、注释或补录任务。若是 ASR 识别错误，先确认正确原词，再修复源转录或字幕，不把模型猜测当成原话。

### 7. 用户确认与主视频剪辑

在用户确认删除、缩短和专业疑点方案后，再用 Script 提交主视频内容编辑。

主视频编辑同时影响画面、嵌入音频、总时长和后续时间线。删除中间内容后，让后续内容顺序前移，不留下主视频黑屏或空档。保留真实交流中的情绪、追问和理解过程；不把对话剪成机械拼接。

### 8. 音频衔接和内容锁定

内容剪辑完成后检查：

- 剪辑点是否爆音、断音或突兀
- 是否保留必要自然呼吸
- 是否有过长但无意义的停顿
- 音画是否仍同步

必要时使用短音频交叉淡化。完成后将主视频时长视为最终基准，称为“主内容锁定”。

### 9. 刷新字幕

主内容锁定后重新生成或刷新字幕：

- 使用最终视频时间线
- 保留用户确认过的文字修改
- 保留用户最后确认的字体、字号、位置和一行布局
- 检查字幕不覆盖人物、界面重点和底部进度条
- 检查字幕不延伸到黑屏或视频结尾之后

### 10. 添加和对齐视觉包装

只有字幕和主内容都确定后，才添加或重排：

- 黄色外扩边框
- 底部进度条
- 开头动画
- 章节提示
- 注释卡片
- 重点强调动画

对齐规则：

- 全程外框从主视频开始到主视频结束。
- 进度条长度和主视频最终时长一致。
- 与内容绑定的注释和动画跟随对应语句移动。
- 删除的语音对应的动画、注释和字幕不能残留。
- 包装层不能遮挡人物、字幕或界面重点。
- 单个项目某个时间点的偶发视觉问题只作为校验发现后修复，不写成固定假设。

### 11. 最终验证

至少抽查开头、中段和结尾的实际合成画面，并检查：

- 画面和音频同步
- 字幕和说话内容同步
- 没有黑屏、空档、重复片段或尾部残留
- 外框四边完整，且不遮挡内容
- 进度条从头到尾连续推进
- 开头动画和注释落在正确内容上
- 所有视觉层都在最终主视频范围内

视觉检查只报告实际发现的问题，不预设某个固定时间一定会出错。

## 交付内容

根据用户要求交付：

- 可继续编辑的 ChatCut 时间线
- 编号版 Markdown 逐字稿
- 内容审查与专业疑点 Review 结果
- 删除、保留和调整记录
- 最终时长与验证摘要

未收到导出指令时，不自动生成 MP4。

## ChatCut 执行顺序

适用时按以下顺序读取和调用相关技能：

1. `chatcut:chatcut-plugin-basics`：读取项目和时间线。
2. `chatcut:transcription`：读取字幕、逐字稿和 Script。
3. `chatcut:talking-head-guide`：执行口播清理、语义剪辑和音频衔接。
4. 完成内容锁定后，使用字幕工具刷新字幕。
5. 如需 Motion Graphics，最后读取并使用 `chatcut:create-motion-graphics`。
6. 最后使用 `chatcut:verification` 检查项目结构和实际合成画面。

一次只推进一个写入阶段。任何写入成功后重新读取最新项目状态；长任务超时后先等待并确认后台状态，禁止并发重试。
